import pandas as pd
import streamlit as st


def render(df: pd.DataFrame) -> None:  # noqa: ARG001 — df wird in Step 2 für die Regression genutzt
    st.info("Treiberanalyse — noch nicht implementiert")
